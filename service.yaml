apiVersion: v1
kind: Service
metadata:
  name: invoice-service
spec:
  type: NodePort
  selector:
    app: invoice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007